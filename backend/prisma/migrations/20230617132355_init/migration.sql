-- CreateTable
CREATE TABLE "Dia" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "rotina" TEXT NOT NULL
);

-- CreateTable
CREATE TABLE "Noite" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "rotina" TEXT NOT NULL
);
